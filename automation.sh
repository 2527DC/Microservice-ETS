#!/bin/bash

# This script will first run the migration  and generation prisma orm commands
# after the successfull of the command running those the docker container will get build and run and in the 


cd packages/database || exit