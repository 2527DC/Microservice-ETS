name: Hello Chethan check

# Allow manual trigger
on:
    workflow_dispatch:

jobs:
    say-hello:
        runs-on: ubuntu-latest

        steps:
            - name: Print Hello
              run: echo "Hello Chethan"
